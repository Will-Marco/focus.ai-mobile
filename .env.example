# Focus AI — M8 maxfiy konfiguratsiya (gitignore'da). Qiymatlarni to'ldiring.
# Supabase: Project Settings → API
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Google Sign In: Google Cloud → Credentials → OAuth Web client ID
# (Supabase Auth → Providers → Google'ga ham shu Web client ID/secret kiritiladi)
GOOGLE_WEB_CLIENT_ID=
